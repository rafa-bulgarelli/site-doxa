/**
 * ESQUELETO do prelude — a track das cenas substitui este arquivo inteiro.
 * O quadro "que foto serve / que foto não serve" do capítulo do clone.
 */
export default function ExemplosDeFotos() {
  return <div aria-hidden className="h-48 rounded-2xl bg-doxa-surface" />;
}
