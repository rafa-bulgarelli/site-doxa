/**
 * ESQUELETO do prelude — a track de cenas substitui este arquivo inteiro.
 * O svg vazio (não `null`) é o que deixa a track do fluxo provar em teste
 * que o slot do passo está ligado antes de a cena real existir.
 */
export default function UmCanal() {
  return <svg aria-hidden viewBox="0 0 480 150" className="h-32 w-full sm:h-40" />;
}
