/**
 * ESQUELETO do prelude — a track das cenas substitui este arquivo inteiro.
 * Existe para o contrato compilar e para capítulo sem cena pronta não quebrar.
 */
export default function CenaGarantia() {
  return <div aria-hidden className="h-40 rounded-2xl bg-doxa-surface" />;
}
