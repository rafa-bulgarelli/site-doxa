# Handoff <YYYY-MM-DD> — <assunto>

## O que funcionou (com evidência)

- <o que ficou pronto> — evidência: `<comando>` → `<saída resumida>` · `<arquivo:linha>` · `<SHA>`

## O que NÃO funcionou (erro exato + causa)

- <tentativa> → **erro:** `<mensagem literal>` · **causa:** <o porquê, se descoberto;
  "causa não identificada" é aceitável — "não deu certo" NÃO é>

<!-- Esta seção é a que mais vale. Sem o erro exato, a próxima sessão refaz a mesma
     tentativa fracassada do zero. -->

## Próximo passo exato

1. <comando ou decisão concreta — não "continuar a feature">

## Estado da torre

- **Tracks abertas:** <branch → executor → status>
- **Mergeado:** <branches em main>
- **Falta VALIDAR-LIVE:** <o que está mergeado mas não conferido no ambiente real>
- **Tick do watchdog:**

```
<cole a saída de .claude/tower/bin/tower-watch.sh>
```

## Decisões tomadas nesta sessão

- <decisão + porquê, para o dono poder discordar depois>
