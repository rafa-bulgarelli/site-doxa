<?xml version="1.0" encoding="UTF-8"?>
<!--
  UMA URL SÓ, e é o número certo.

  O site é uma página única: tudo que a Doxa oferece — a garantia, a prova, a
  comparação, o formulário e o FAQ — mora em `/`. As âncoras (`#forms`, `#faq`)
  não entram aqui: sitemap lista documentos, e um fragmento não é um documento.

  `/leads` também não entra, de propósito. É a Central do time, atrás de senha,
  e um sitemap é onde se diz ao buscador "isto eu QUERO que apareça" — a tela
  interna não é isso. O `robots.txt` não a bloqueia, então ela segue rastreável
  se alguém apontar um link para lá; se um dia isso incomodar, a correção é uma
  linha `Disallow: /leads` no robots, e não uma ausência aqui.

  `lastmod` é a data da última mudança de CONTEÚDO da página, não do último
  deploy. Mexer em CSS, corrigir um bug ou publicar de novo não muda esta linha
  — o buscador aprende a ignorar sitemaps cujo lastmod muda a cada push.
-->
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
  <url>
    <loc>https://www.doxaviral.com/</loc>
    <lastmod>2026-08-10</lastmod>
    <changefreq>weekly</changefreq>
    <priority>1.0</priority>
  </url>
</urlset>
